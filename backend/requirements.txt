fastapi>=0.111
uvicorn[standard]>=0.30
sqlalchemy>=2.0
psycopg2-binary>=2.9
pandas>=2.0
python-dateutil>=2.9
pytz>=2024.1
tzdata>=2024.1
numpy>=1.26
scikit-learn>=1.4
lightgbm>=4.3
joblib>=1.3
email-validator==2.3.0
loguru>=0.7
httpx>=0.27
python-dotenv>=1.0
bcrypt>=4.1
itsdangerous>=2.1
jinja2>=3.1
weasyprint>=62
ydata-profiling>=4.12
# ydata-profiling still imports the legacy pkg_resources API at import time; setuptools 82+
# dropped it entirely (and its own deprecation warning recommends pinning <81), and
# python:3.12-slim doesn't ship setuptools by default, so leaving this unpinned breaks the
# backend image build outright once pip resolves a setuptools >=82.
setuptools<81
